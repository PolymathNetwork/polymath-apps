---
name: FormItemGroup
---

import { PropsTable } from 'docz';
import { Formik, FastField } from 'formik';
import * as Yup from 'yup';

import Playground from '../DoczPlayground';
import TextInput from '../inputs/TextInput';
import FormItem from '../FormItem';
import FormError from '../FormError';
import FormItemGroup from './';

# FormItem

<PropsTable of={FormItem} />

## Basic usage

<Playground>
  <Formik
    validationSchema={Yup.object().shape({
      firstname: Yup.string().required(),
      lastname: Yup.string().required(),
    })}
    render={() => (
      <FormItemGroup>
        <FormItemGroup.Items>
          <FormItem name="firstname">
            <FormItem.Label>Firstname</FormItem.Label>
            <FormItem.Input
              formikComponent={FastField}
              component={TextInput}
              placeholder="firstname"
            />
          </FormItem>
          <FormItem name="lastname">
            <FormItem.Label>lastname</FormItem.Label>
            <FormItem.Input
              formikComponent={FastField}
              component={TextInput}
              placeholder="lastname"
            />
          </FormItem>
        </FormItemGroup.Items>

        <FormError name="firstname" />
        <FormError name="lastname" />
      </FormItemGroup>
    )}
  />
</Playground>
