import React from 'react';
import styled from 'styled-components';

import Box from '../Box';

const InlineFlex = styled(Box)`
  display: inline-flex;
  align-items: center;
`;

export default InlineFlex;
