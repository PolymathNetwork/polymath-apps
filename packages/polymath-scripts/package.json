{
  "name": "@polymathnetwork/scripts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "local-blockchain:start": "ganache-cli -i 15 --gasLimit 90000000",
    "local-blockchain:migrate": "truffle migrate --network=development --reset --all",
    "local-blockchain:compile": "truffle compile --optimize-runs 200"
  },
  "devDependencies": {
    "ganache-cli": "^6.1.8",
    "polymath-core": "^1.4.0",
    "truffle": "^4.1.14"
  }
}
