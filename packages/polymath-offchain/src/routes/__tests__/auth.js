describe('Route: /auth/:address', () => {
  test('Sends error on invalid address', async () => {});
  test('AuthCode document is properly created');
  test('If request is valid, send proper response');
});
